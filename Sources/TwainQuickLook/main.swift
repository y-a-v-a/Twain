// Unused: the appex is linked with -e _NSExtensionMain (see Package.swift),
// so Foundation's extension runtime is the real entry point. SPM just needs a
// main file to build an executable target.
